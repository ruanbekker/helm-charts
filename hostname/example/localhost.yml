nameOverride: "yellow"
fullnameOverride: "yellow"
replicaCount: 1

ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: traefik 
  hosts:
    - host: "yellow"
      domain: "example.com"
      paths: [/]
